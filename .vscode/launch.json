{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Debug Project (g++)",
            "type": "cppdbg",
            "request": "launch",
            
            // --- THE RELIABLE FIX ---
            // Use \\ for the program path
            "program": "${workspaceFolder}\\my_program.exe",
            
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "C:\\msys64\\ucrt64\\bin\\gdb.exe",
            
            // This runs the task we defined above
            "preLaunchTask": "Build Project (g++)"
        }
    ]
}